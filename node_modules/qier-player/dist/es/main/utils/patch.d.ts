import { IControllerEle } from '../modules/controller';
export declare type TElement = Pick<IControllerEle, 'el' | 'id'>;
export declare function patch(oldElements: (TElement | null)[], newElements: (TElement | null)[], container: HTMLElement, op?: {
    mount?: (element: TElement) => void;
    unmount?: (element: TElement) => void;
}): void;
