import { Dispose } from './dispose';
declare type Fn = (ev: PointerEvent) => any;
export declare class Drag implements Dispose {
    private el;
    private start;
    private move;
    private end;
    private pending;
    private lastEv;
    constructor(el: HTMLElement, start: Fn, move: Fn, end?: Fn);
    private downHandler;
    private upHandler;
    private moveHandler;
    private handlerMove;
    dispose(): void;
}
export {};
