import { Player } from '../player';
import { Dispose } from '../utils/dispose';
export declare class WebFullscreen implements Dispose {
    private player;
    constructor(player: Player);
    get isActive(): boolean;
    enter(): void;
    exit(): boolean;
    toggle: () => void;
    dispose(): void;
}
