import { Player } from '../player';
import { Dispose } from '../utils/dispose';
export declare type ShortcutHandler = (player: Player) => void;
export declare class Shortcut implements Dispose {
    private player;
    private map;
    private timeout;
    constructor(player: Player, disabled?: boolean);
    private onKeyDown;
    register(code: number, handler: ShortcutHandler): void;
    unregister(code: number): boolean;
    enable(): void;
    dispose(): void;
    disable(): void;
}
