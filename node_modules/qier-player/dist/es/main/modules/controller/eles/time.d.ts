import { Player } from '../../../player';
import { DomNode } from '../../../utils/domNode';
import { IControllerEle } from '..';
export declare class Time extends DomNode implements IControllerEle {
    readonly id = "time";
    private playedText;
    private slashText;
    private totalText;
    init(player: Player): void;
    private set played(value);
    private set slash(value);
    private set total(value);
}
export declare const timeControllerEle: () => Time;
