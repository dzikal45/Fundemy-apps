import { Tooltip } from '../../../components/tooltip';
import { Player } from '../../../player';
import { DomNode } from '../../../utils/domNode';
import { IControllerEle } from '..';
declare class WebFullscreen extends DomNode implements IControllerEle {
    readonly id = "web-fullscreen";
    private enterIcon;
    private exitIcon;
    tooltip: Tooltip;
    init(player: Player, tooltip: Tooltip): void;
    private enter;
    private exit;
}
export declare const webFullscreenControllerEle: () => WebFullscreen;
export {};
