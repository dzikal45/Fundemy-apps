import { DomNode } from '../../../utils/domNode';
import { Player } from '../../../player';
import { Tooltip } from '../../../components/tooltip';
import { IControllerEle } from '..';
declare class Play extends DomNode implements IControllerEle {
    readonly id = "play";
    private playIcon;
    private pauseIcon;
    tooltip: Tooltip;
    init(player: Player, tooltip: Tooltip): void;
    private onPlay;
    private onPause;
}
export declare const playControllerEle: () => Play;
export {};
