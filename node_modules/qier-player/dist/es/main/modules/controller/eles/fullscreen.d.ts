import { Tooltip } from '../../../components/tooltip';
import { Player } from '../../../player';
import { DomNode } from '../../../utils/domNode';
import { IControllerEle } from '..';
declare class Fullscreen extends DomNode implements IControllerEle {
    readonly id = "fullscreen";
    private player;
    private exitIcon;
    private enterIcon;
    tooltip: Tooltip;
    mounted: false;
    init(player: Player, tooltip: Tooltip): void;
    private enter;
    private exit;
}
export declare const fullscreenControllerEle: () => Fullscreen;
export {};
