import { IThumbnail } from '../../../types';
import { DomNode } from '../../../utils/domNode';
export interface ThumbImg {
    url: string;
    x: number;
    y: number;
}
export declare class Thumbnail extends DomNode {
    private opts;
    private preImgEl?;
    private timeTagEl;
    private onePreImgNums?;
    private rect;
    constructor(container: HTMLElement, opts: IThumbnail);
    private getCurrentThumb;
    updateOptions(opts: IThumbnail): void;
    updatePos(seconds: number, x: number, maxX: number): void;
}
