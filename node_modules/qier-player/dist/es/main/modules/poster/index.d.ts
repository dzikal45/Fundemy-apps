import { Player } from '../../player';
import { DomNode } from '../../utils/domNode';
export declare class Poster extends DomNode {
    private player;
    private url?;
    constructor(container: HTMLElement, player: Player);
    get isActive(): boolean;
    private tryHide;
    show(): void;
    hide(): void;
}
