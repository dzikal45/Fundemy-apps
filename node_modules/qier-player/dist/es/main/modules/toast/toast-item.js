var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import { addClass } from '../../utils/dom';
import { DomNode } from '../../utils/domNode';
import { isHTMLElement, isString } from '../../utils/is';
var ToastItem = /** @class */ (function (_super) {
    __extends(ToastItem, _super);
    function ToastItem(container, html, position) {
        if (position === void 0) { position = 'center'; }
        var _this = _super.call(this, container, '.toast') || this;
        _this.position = position;
        addClass(_this.el, "toast_" + position);
        if (isHTMLElement(html))
            _this.el.appendChild(html);
        if (isString(html))
            _this.el.innerHTML = html;
        return _this;
    }
    ToastItem.prototype.dispose = function () {
        _super.prototype.dispose.call(this);
        clearTimeout(this.timer);
    };
    return ToastItem;
}(DomNode));
export { ToastItem };
//# sourceMappingURL=toast-item.js.map