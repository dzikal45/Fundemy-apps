import { Dispose } from '../../utils/dispose';
import { ToastItem } from './toast-item';
export declare type Position = 'center' | 'left_top' | 'right_top' | 'left_bottom' | 'right_bottom';
export declare class Toast implements Dispose {
    private toasts;
    private container;
    constructor(container: HTMLElement);
    show(html: string | HTMLElement, position?: Position, timeout?: number): ToastItem;
    close(toastItem?: ToastItem): void;
    dispose(): void;
}
